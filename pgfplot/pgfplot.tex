\documentclass[border=10pt]{standalone}
\usepackage{upgreek}
\usepackage[american]{circuitikz}
\usetikzlibrary{patterns}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\begin{document}

\begin{tikzpicture}
    \begin{axis}[
        title=$V_{out}$ vs. $P_{in}$ at 8.4 GHz,
        xlabel={$P_{in}$ (dBm)},
        ylabel={$V_{out}$ (V)},
        grid=major,
        xmin=-60, xmax=0,
        ymin=0, ymax=3,
        xtick={-60,-50,...,0},
        ytick={0,0.5,...,3},
        legend style = {
            align = left,
            cells = {anchor=east},
            legend pos=outer north east,
        }
    ]
    % Can also use x index, y index; index starts at column 0
    \addplot[smooth, blue] table [x=Pin,y=Vout] {Data/Vout vs Pin.txt};
    
    \end{axis}
\end{tikzpicture}

\end{document}